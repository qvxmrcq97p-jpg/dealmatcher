#!/usr/bin/env bash
# Wrapper that runs the status dashboard without exposing .py to chat link-mangling.
# Run from anywhere: bash ~/dealmatcher/status
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
exec python3 "$SCRIPT_DIR/tools/mba_status_dashboard.py" "$@"
