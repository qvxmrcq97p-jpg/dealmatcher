name = "cc-events-worker"
main = "cc_events_worker.js"
compatibility_date = "2024-10-01"

# KV namespace for /health endpoint last_event_at timestamp.
# Create once with:
#   wrangler kv namespace create LAST_EVENT_AT_CC
# Then paste the returned id below and re-deploy.
[[kv_namespaces]]
binding = "LAST_EVENT_AT"
id      = "33370d5f0973430fa14956a03cfdc0d3"
